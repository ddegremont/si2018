{% extends '@admin/layout.html.twig' %}

{% block title %}Message{% endblock %}

{% block main %}
    <table class="table">
        <tbody>
            <tr>
                <th>Status</th>
                <td>{{ message.Status }}</td>
            </tr>
            <tr>
                <th>Company</th>
                <td>{{ message.Company }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ message.Name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ message.Email }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ message.phone }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ message.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_message_index') }}" class="btn btn-primary">Retour à la liste</a>

    <hr>

    {{ include('@admin/message/_delete_form.html.twig') }}
{% endblock %}