{% extends '@admin/layout.html.twig' %}

{% block title %}Compagnie aérienne{% endblock %}

{% block main %}
    <div class="form-group">
        <strong>Twitter : </strong>{{ company.twitter }}
    </div>

    <a href="{{ path('admin_company_edit', {'id': company.id}) }}" class="btn btn-primary">Modifier</a>

    <a href="{{ path('admin_company_index') }}" class="btn btn-primary">Retour à la liste</a>

    <hr>

    {{ include('@admin/company/_delete_form.html.twig') }}
{% endblock %}