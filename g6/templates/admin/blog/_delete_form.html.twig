{{ include('@admin/blog/_delete_post_confirmation.html.twig') }}

<form action="{{ url('admin_post_delete', {id: post.id}) }}" method="post" data-confirmation="true" id="delete-form">
    <input type="hidden" name="token" value="{{ csrf_token('delete') }}" />
    <button type="submit" class="btn btn-danger" data-toggle="modal" data-target="#confirmationModal">
        <i class="fa fa-trash" aria-hidden="true"></i>
        Supprimer
    </button>
</form>
