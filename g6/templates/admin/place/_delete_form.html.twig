<form method="post" action="{{ path('admin_place_delete', {'id': place.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ place.id) }}">
    <button class="btn btn-danger">Supprimer</button> <a href="{{ path('admin_place_index') }}" class="btn btn-primary">Retour à la liste</a>
</form>