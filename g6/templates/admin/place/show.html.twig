{% extends '@admin/layout.html.twig' %}

{% block title %}Commerce{% endblock %}

{% block main %}
    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ place.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ place.type }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ place.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_place_edit', {'id': place.id}) }}" class="btn btn-primary">Modifier</a>

    <hr>

    {{ include('@admin/place/_delete_form.html.twig') }}
{% endblock %}